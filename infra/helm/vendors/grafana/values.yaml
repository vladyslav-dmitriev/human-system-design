podSecurityPolicy:
  enabled: false

persistence:
  enabled: false

service:
  type: ClusterIP

rbac:
  create: true
  pspEnabled: false
  namespaced: true 

global:
  podSecurityPolicy:
    enabled: false
  podSecurityStandards:
    enabled: true
    enforce: restricted
